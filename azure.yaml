# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: sme-predictive-analytics
metadata:
  template: sme-predictive-analytics@0.0.1-beta

services:
  backend:
    project: ./backend
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
  
  ml-services:
    project: ./ml-models
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
  
  frontend:
    project: ./frontend
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
